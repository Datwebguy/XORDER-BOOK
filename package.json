{
  "name": "xorders-monorepo",
  "private": true,
  "version": "0.1.0",
  "description": "XOrders: Uniswap v4 stop-loss and take-profit hook dashboard for X Layer.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build",
    "lint": "npm --prefix frontend run lint",
    "contracts:build": "cd contracts && forge build",
    "contracts:test": "cd contracts && forge test",
    "deploy:xlayer": "cd contracts && forge script script/DeployXOrders.s.sol:DeployXOrders --rpc-url https://rpc.xlayer.tech --chain-id 196 --broadcast --verify"
  },
  "workspaces": [
    "frontend"
  ]
}
